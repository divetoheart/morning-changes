# Archived static release

The pre-React Morning Changes application is preserved exactly as it existed before the production migration on:

- Branch: `archive/static-pre-react-2026-06-30`

That branch retains the full static Pages app, deployment workflow, lesson shell, After Hours guide integration, mobile styling, and associated assets.

The `main` branch is now reserved for the production React + TypeScript application. The legacy branch is not a development branch; it exists for reference, recovery, and comparison.
